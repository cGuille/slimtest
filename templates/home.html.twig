{% extends 'layout.html.twig' %}

{% block title %}Home – {{ parent() }}{% endblock %}

{% block content %}
  <p>
    Hello! You are logged in as {{ user.getEmail() }}.
  </p>
  <p>
    <a href="{{ urlFor('logout.process') }}">Log out</a>.
  </p>
{% endblock %}
